using System.Collections.Generic;
using UnityEngine;

public struct View
{   
    //public Item CheckCollosionPostion(IEnumerable<Item> item, Vector3 click)
    //{
    //    foreach (var collections in item)
    //    {
    //        if (Distance(click, collections.Point) <= collections.Range)
    //        {
    //            return collections;
    //        }
    //        else
    //        {
    //            break;
    //        }
    //    }    
    //}
    //private float Distance(Vector3 point, Vector2 centerСircle)
    //{
    //    return ((point.x - centerСircle.x) + (point.y - centerСircle.y)) * 2;
    //}
}
